name: gamification_flutter_sdk
description: Flutter SDK for the SaaS Gamification Platform. Auto-tracking via gamif_scanner.
version: 1.0.1
homepage: https://github.com/louayabidi/gamification_flutter_sdk
repository: https://github.com/louayabidi/gamification_flutter_sdk

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0